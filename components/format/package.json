{
  "name": "@rum-profiler/format",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The versioned, self-describing capture format: schema, manifest, and binary codec.",
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "//sideEffects": "All modules are pure (definitions only, no top-level side effects), so bundlers may drop unused exports — this is what tree-shakes the decode side off the on-page pack path. See components/format/README.md (Codec → On-page footprint).",
  "sideEffects": false,
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -b"
  }
}
